package com.app.aero.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {

    // Brand
    val Primary = Color(0xFF0058BD)
    val PrimaryContainer = Color(0xFF2771DF)

    val Secondary = Color(0xFF006E1C)
    val SecondaryContainer = Color(0xFF4CAF50)

    val Tertiary = Color(0xFFB81311)
    val TertiaryContainer = Color(0xFFF44336)

    val Neutral = Color(0xFF424242)

    // Surfaces (Tonal layering)
    val Surface = Color(0xFFFBF9F8)
    val SurfaceLow = Color(0xFFF5F3F3)
    val SurfaceLowest = Color(0xFFFFFFFF)
    val SurfaceHighest = Color(0xFFE4E2E1)

    // Text
    val OnSurface = Color(0xFF1B1C1C)
    val OnSurfaceVariant = Color(0xFF424753)

    // Outline (Ghost border)
    val OutlineVariant = Color(0x26424753) // 15% opacity
}
object AppDarkColors {

    val Primary = Color(0xFF4C8DFF)
    val PrimaryContainer = Color(0xFF6EA8FF)

    val Secondary = Color(0xFF66BB6A)
    val SecondaryContainer = Color(0xFF2E7D32)

    val Tertiary = Color(0xFFFF6B6B)
    val TertiaryContainer = Color(0xFFB71C1C)

    // Deep Carbon Layering
    val Surface = Color(0xFF303030)
    val SurfaceLow = Color(0xFF383838)
    val SurfaceLowest = Color(0xFF424242)
    val SurfaceHighest = Color(0xFF4A4A4A)

    val OnSurface = Color(0xFFEDEDED)
    val OnSurfaceVariant = Color(0xFFB0B0B0)

    val OutlineVariant = Color(0x33FFFFFF) // subtle light border
}